<%- include('partials/header') %>

<div class="container mb-3">
  <div class="jumbotron">
    <div class="text-center">
      <h1 class="display-4 text-center">Welcome!</h1>
      <hr class="my-4" />
      <% if(!currentUser){ %>
      <p>Log In to use the App</p>
      <a href="/register" class="btn btn-primary btn-lg" role="button"
        >Sign Up</a
      >
      <a href="/login" class="btn btn-primary btn-lg" role="button">Log In</a>
      <% } else {%>
      <a href="/grades" class="btn btn-primary btn-lg">Classes</a>
      <% } %>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
